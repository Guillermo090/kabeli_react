// Objeto
const producto = {
    nombre : "Tablet",
    precio : 300,
    disponible: false
}

// Object.freeze(producto); // Congela el objeto, no se pueden agregar ni eliminar propiedades
//Object.seal(producto); // Sealed congela el objeto pero permite modificar las propiedades existentes

